ENV=production
# Production API (Docker / Play / live web)
API_BASE_URL=https://api.kmcalumni.net
API_PREFIX=/api/v1
# Local/LAN backend instead:
# ENV=development
# API_BASE_URL=http://200.141.2.90:8001
# Public Razorpay key only — never put RAZORPAY_KEY_SECRET in the frontend
RAZORPAY_KEY_ID=rzp_live_TGwiguT5HZKaNR
# Optional storage bucket name (API serves files at /media/{bucket}/...)
STORAGE_BUCKET=verification-documents
# Legacy only — not required when backend uses DATABASE_URL + local media
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_STORAGE_BUCKET=verification-documents
